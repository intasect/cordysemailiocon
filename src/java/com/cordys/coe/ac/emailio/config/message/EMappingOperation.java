package com.cordys.coe.ac.emailio.config.message;

/**
 * Holds all the different mapping operations that are possible.
 *
 * @author  pgussow
 */
public enum EMappingOperation
{
    STRING_REPLACE,
    XML_APPEND_CHILD,
    XML_APPEND_CHILD_WITH_TARGET_NS,
    XML_REPLACE,
    XML_REPLACE_WITH_TARGET_NS,
    CUSTOM
}
